import { Link, useForm } from '@inertiajs/react';
import { useMemo, useState } from 'react';
import AuthenticatedLayout from '../../Layouts/AuthenticatedLayout';
import Card from '../../Components/Card';
import FormField from '../../Components/FormField';
import FormSection from '../../Components/FormSection';
import PageHeader from '../../Components/PageHeader';
import PrimaryButton from '../../Components/PrimaryButton';

const selectClass = 'mt-1.5 block w-full rounded-ui border-gray-300 bg-white shadow-sm focus:border-green-700 focus:ring-green-700 dark:border-gray-600 dark:bg-gray-900 dark:text-white';

export default function Form({ title, managementPlan, protectedAreas, planTypes, statuses }) {
    const isEdit = Boolean(managementPlan);
    const form = useForm({ protected_area_id: managementPlan?.protected_area_id || '', plan_type: managementPlan?.plan_type || '', title: managementPlan?.title || '', version: managementPlan?.version || '', prepared_year: managementPlan?.prepared_year || '', approval_date: managementPlan?.approval_date || '', valid_from: managementPlan?.valid_from || '', valid_until: managementPlan?.valid_until || '', status: managementPlan?.status || 'Draft', remarks: managementPlan?.remarks || '', attachment: null });
    const [areaSearch, setAreaSearch] = useState(managementPlan?.protected_area_name || '');
    const availableAreas = useMemo(() => protectedAreas.filter((area) => area.name.toLowerCase().includes(areaSearch.toLowerCase()) || String(area.id) === String(form.data.protected_area_id)), [protectedAreas, areaSearch, form.data.protected_area_id]);
    const submit = (event) => { event.preventDefault(); isEdit ? form.post(`/management-plans/${managementPlan.id}`, { _method: 'patch', forceFormData: true }) : form.post('/management-plans', { forceFormData: true }); };
    const select = (id, label, options) => <label className="block text-sm font-medium text-gray-700 dark:text-gray-200" htmlFor={id}>{label}<select id={id} className={selectClass} value={form.data[id]} onChange={(event) => form.setData(id, event.target.value)}>{options}</select>{form.errors[id] && <p className="mt-1.5 text-sm text-red-700 dark:text-red-300">{form.errors[id]}</p>}</label>;

    return <AuthenticatedLayout title={title}><PageHeader title={title} description={isEdit ? 'Update this management plan record without changing prior plan versions.' : 'Register a new management plan version for a protected area.'} actions={<Link href="/management-plans" className="text-sm font-semibold text-green-800 hover:text-green-950 dark:text-green-400">← Back to management plans</Link>} /><Card className="mt-6 max-w-5xl"><form onSubmit={submit} className="space-y-8"><FormSection title="Plan Details" description="Select an existing protected area and identify the plan version."><div className="grid gap-5 sm:grid-cols-2"><div className="sm:col-span-2"><label htmlFor="protected-area-search" className="block text-sm font-medium text-gray-700 dark:text-gray-200">Protected Area</label><input id="protected-area-search" type="search" value={areaSearch} onChange={(event) => setAreaSearch(event.target.value)} placeholder="Search protected areas" className={selectClass} /><select required aria-label="Protected Area" className={selectClass} value={form.data.protected_area_id} onChange={(event) => { form.setData('protected_area_id', event.target.value); setAreaSearch(protectedAreas.find((area) => String(area.id) === event.target.value)?.name || ''); }}><option value="">Select a protected area</option>{availableAreas.map((area) => <option key={area.id} value={area.id}>{area.name}</option>)}</select>{form.errors.protected_area_id && <p className="mt-1.5 text-sm text-red-700 dark:text-red-300">{form.errors.protected_area_id}</p>}</div>{select('plan_type', 'Plan Type', <><option value="">Select a plan type</option>{planTypes.map((type) => <option key={type}>{type}</option>)}</>)}<FormField id="title" label="Title" value={form.data.title} onChange={(event) => form.setData('title', event.target.value)} error={form.errors.title} required /><FormField id="version" label="Version" value={form.data.version} onChange={(event) => form.setData('version', event.target.value)} error={form.errors.version} placeholder="e.g., Version 2" required /><FormField id="prepared_year" label="Prepared Year" type="number" min="1800" max={new Date().getFullYear() + 10} value={form.data.prepared_year} onChange={(event) => form.setData('prepared_year', event.target.value)} error={form.errors.prepared_year} required />{select('status', 'Status', statuses.map((status) => <option key={status}>{status}</option>))}</div></FormSection><FormSection title="Approval and Validity"><div className="grid gap-5 sm:grid-cols-3"><FormField id="approval_date" label="Approval Date" type="date" value={form.data.approval_date} onChange={(event) => form.setData('approval_date', event.target.value)} error={form.errors.approval_date} /><FormField id="valid_from" label="Validity Start" type="date" value={form.data.valid_from} onChange={(event) => form.setData('valid_from', event.target.value)} error={form.errors.valid_from} /><FormField id="valid_until" label="Validity End" type="date" value={form.data.valid_until} onChange={(event) => form.setData('valid_until', event.target.value)} error={form.errors.valid_until} /></div></FormSection><FormSection title="Attachment and Remarks"><div className="grid gap-5"><label className="block text-sm font-medium text-gray-700 dark:text-gray-200" htmlFor="attachment">Attachment<input id="attachment" type="file" className={selectClass} onChange={(event) => form.setData('attachment', event.target.files[0])} />{managementPlan?.attachment && <span className="mt-1.5 block text-xs font-normal text-gray-500 dark:text-gray-400">A file is already attached. Select a new file only to replace it.</span>}{form.errors.attachment && <p className="mt-1.5 text-sm text-red-700 dark:text-red-300">{form.errors.attachment}</p>}</label><label className="block text-sm font-medium text-gray-700 dark:text-gray-200" htmlFor="remarks">Remarks<textarea id="remarks" rows="4" className={selectClass} value={form.data.remarks} onChange={(event) => form.setData('remarks', event.target.value)} />{form.errors.remarks && <p className="mt-1.5 text-sm text-red-700">{form.errors.remarks}</p>}</label></div></FormSection><div className="flex flex-wrap gap-3 border-t border-gray-200 pt-5 dark:border-gray-700"><PrimaryButton type="submit" disabled={form.processing}>{form.processing ? 'Saving...' : isEdit ? 'Save changes' : 'Create management plan'}</PrimaryButton><Link href="/management-plans" className="inline-flex items-center rounded-ui px-4 py-2 text-sm font-semibold text-gray-700 hover:bg-gray-100 dark:text-gray-200 dark:hover:bg-gray-800">Cancel</Link></div></form></Card></AuthenticatedLayout>;
}
