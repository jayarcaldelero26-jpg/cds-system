import { Link, router, usePage } from '@inertiajs/react';
import { useEffect, useState } from 'react';
import AuthenticatedLayout from '../../Layouts/AuthenticatedLayout';
import Card from '../../Components/Card';
import ConfirmDialog from '../../Components/ConfirmDialog';
import DataTable from '../../Components/DataTable';
import PageHeader from '../../Components/PageHeader';
import StatusBadge from '../../Components/StatusBadge';

const variants = { Draft: 'pending', Active: 'active', Expired: 'inactive', 'For Updating': 'info', Archived: 'inactive' };
const messages = { 'management-plan-created': 'Management plan created successfully.', 'management-plan-updated': 'Management plan updated successfully.', 'management-plan-deleted': 'Management plan deleted successfully.' };

export default function Index({ managementPlans, filters, protectedAreas, planTypes, statuses }) {
    const { auth, status } = usePage().props;
    const [search, setSearch] = useState(filters.search || '');
    const [deleting, setDeleting] = useState(null);
    useEffect(() => setSearch(filters.search || ''), [filters.search]);
    const visit = (params) => router.get('/management-plans', { ...filters, search, ...params }, { preserveState: true, replace: true });
    const remove = () => router.delete(`/management-plans/${deleting.id}`, { onFinish: () => setDeleting(null) });
    const columns = [{ key: 'protected_area_name', label: 'Protected Area', render: (plan) => <span className="font-medium text-gray-900 dark:text-white">{plan.protected_area_name}</span> }, { key: 'plan_type', label: 'Plan Type' }, { key: 'version', label: 'Version' }, { key: 'prepared_year', label: 'Prepared Year' }, { key: 'status', label: 'Status', render: (plan) => <StatusBadge variant={variants[plan.status]}>{plan.status}</StatusBadge> }, { key: 'validity', label: 'Validity', render: (plan) => plan.valid_from || plan.valid_until ? `${plan.valid_from || '—'} – ${plan.valid_until || '—'}` : '—' }, { key: 'actions', label: <span className="sr-only">Actions</span>, cellClassName: 'text-right', render: (plan) => <div className="flex justify-end gap-3">{auth.canUpdateManagementPlans && <Link className="font-medium text-green-800 hover:text-green-950 dark:text-green-400" href={`/management-plans/${plan.id}/edit`}>Edit</Link>}{auth.canDeleteManagementPlans && <button type="button" className="font-medium text-red-700 hover:text-red-900 dark:text-red-300" onClick={() => setDeleting(plan)}>Delete</button>}</div> }];
    const selectClass = 'mt-1.5 block w-full rounded-ui border-gray-300 bg-white px-3 py-2 text-sm shadow-sm focus:border-green-700 focus:ring-green-700 dark:border-gray-600 dark:bg-gray-900 dark:text-white';

    return <AuthenticatedLayout title="Management Plans"><PageHeader title="Management Plans" description="Versioned management plans for DENR PENRO Mati protected areas." actions={auth.canCreateManagementPlans && <Link href="/management-plans/create" className="inline-flex items-center justify-center rounded-lg bg-green-800 px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:bg-green-900">Add management plan</Link>} />{messages[status] && <p className="mt-6 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800 dark:border-emerald-900 dark:bg-emerald-950 dark:text-emerald-200">{messages[status]}</p>}<Card className="mt-6" padding="p-0"><form onSubmit={(event) => { event.preventDefault(); visit({ page: 1 }); }} className="grid gap-3 border-b border-gray-200 p-4 dark:border-gray-700 md:grid-cols-4"><label className="md:col-span-2"><span className="text-sm font-medium text-gray-700 dark:text-gray-200">Search</span><input type="search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Protected area, type, status, or year" className={selectClass} /></label><label><span className="text-sm font-medium text-gray-700 dark:text-gray-200">Protected Area</span><select className={selectClass} value={filters.protected_area_id || ''} onChange={(event) => visit({ protected_area_id: event.target.value, page: 1 })}><option value="">All protected areas</option>{protectedAreas.map((area) => <option key={area.id} value={area.id}>{area.name}</option>)}</select></label><label><span className="text-sm font-medium text-gray-700 dark:text-gray-200">Plan Type</span><select className={selectClass} value={filters.plan_type || ''} onChange={(event) => visit({ plan_type: event.target.value, page: 1 })}><option value="">All plan types</option>{planTypes.map((type) => <option key={type}>{type}</option>)}</select></label><label><span className="text-sm font-medium text-gray-700 dark:text-gray-200">Status</span><select className={selectClass} value={filters.status || ''} onChange={(event) => visit({ status: event.target.value, page: 1 })}><option value="">All statuses</option>{statuses.map((item) => <option key={item}>{item}</option>)}</select></label><div className="flex items-end"><button type="submit" className="w-full rounded-ui bg-green-800 px-4 py-2 text-sm font-semibold text-white hover:bg-green-900">Search</button></div></form><DataTable columns={columns} rows={managementPlans.data} emptyTitle="No management plans found" emptyDescription="Create a plan record or adjust the filters." /></Card><div className="mt-5 flex justify-between text-sm">{managementPlans.prev_page_url ? <Link href={managementPlans.prev_page_url} className="font-semibold text-green-800 dark:text-green-400">← Previous</Link> : <span />}{managementPlans.next_page_url ? <Link href={managementPlans.next_page_url} className="font-semibold text-green-800 dark:text-green-400">Next →</Link> : <span />}</div><ConfirmDialog open={Boolean(deleting)} title="Delete management plan?" message={`Remove ${deleting?.title} from the active registry? This plan record is soft deleted.`} confirmLabel="Delete management plan" onCancel={() => setDeleting(null)} onConfirm={remove} /></AuthenticatedLayout>;
}
