import { Link } from '@inertiajs/react';
const classes = active => `inline-flex shrink-0 items-center rounded-xl border px-4 py-2.5 text-xs font-bold transition ${active ? 'border-green-700 bg-green-700 text-white shadow-md' : 'border-gray-200 bg-white text-gray-700 shadow-sm hover:border-green-300 hover:bg-green-50 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-300'}`;
export default function WorkflowTabs({ active, showReport = true, showData = true, showMaintenance = true }) { return <nav aria-label="IMEA workflows" className="mt-3 flex gap-1.5 overflow-x-auto">{showReport && <Link href={route('imea.report-submissions.index')} className={classes(active === 'submissions')}>IMEA Report</Link>}{showData && <Link href={route('imea.index')} className={classes(active === 'facilities')}>Facilities &amp; Infrastructures Inventory</Link>}{showMaintenance && <Link href={route('imea.maintenance-reports.index')} className={classes(active === 'maintenance')}>Facility Maintenance Reports</Link>}</nav>; }
