import { useForm } from '@inertiajs/react'; import GuestLayout from '../../Layouts/GuestLayout';
export default function ConfirmPassword(){const {data,setData,post,processing,errors}=useForm({password:''});return <GuestLayout title="Confirm Password"><p className="text-sm dark:text-gray-300">Confirm your password to continue.</p><form onSubmit={e=>{e.preventDefault();post('/confirm-password')}} className="mt-4 space-y-4"><input className="w-full rounded border-gray-300" type="password" value={data.password} onChange={e=>setData('password',e.target.value)}/>{errors.password&&<p className="text-sm text-red-600">{errors.password}</p>}<button disabled={processing} className="rounded bg-gray-800 px-4 py-2 text-xs font-semibold uppercase text-white">Confirm</button></form></GuestLayout>}
