import { Link, useForm } from '@inertiajs/react'; import GuestLayout from '../../Layouts/GuestLayout';
export default function ForgotPassword({status}){const {data,setData,post,processing,errors}=useForm({email:''});return <GuestLayout title="Forgot Password"><p className="text-sm dark:text-gray-300">Enter your email to receive a reset link.</p>{status&&<p className="mt-3 text-sm text-green-600">{status}</p>}<form onSubmit={e=>{e.preventDefault();post('/forgot-password')}} className="mt-4 space-y-4"><input className="w-full rounded border-gray-300" type="email" placeholder="Email" value={data.email} onChange={e=>setData('email',e.target.value)}/>{errors.email&&<p className="text-sm text-red-600">{errors.email}</p>}<button disabled={processing} className="rounded bg-gray-800 px-4 py-2 text-xs font-semibold uppercase text-white">Email reset link</button></form><Link className="mt-4 block text-sm" href="/login">Back to login</Link></GuestLayout>}
