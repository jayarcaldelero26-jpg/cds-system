import { Link, useForm } from '@inertiajs/react'; import GuestLayout from '../../Layouts/GuestLayout';
export default function Register(){const {data,setData,post,processing,errors}=useForm({name:'',email:'',password:'',password_confirmation:''});const submit=e=>{e.preventDefault();post('/register')};return <GuestLayout title="Register"><h1 className="text-xl font-semibold dark:text-white">Register</h1><form onSubmit={submit} className="mt-6 space-y-4">{[['name','Name','text'],['email','Email','email'],['password','Password','password'],['password_confirmation','Confirm password','password']].map(([key,label,type])=><div key={key}><input className="w-full rounded border-gray-300" type={type} placeholder={label} value={data[key]} onChange={e=>setData(key,e.target.value)}/>{errors[key]&&<p className="text-sm text-red-600">{errors[key]}</p>}</div>)}<button disabled={processing} className="rounded bg-gray-800 px-4 py-2 text-xs font-semibold uppercase text-white">Register</button></form><Link className="mt-4 block text-sm" href="/login">Already registered?</Link></GuestLayout>}
